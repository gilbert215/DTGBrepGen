# Core scientific packages
numpy==1.26.4
pandas==2.2.2
matplotlib==3.9.0
scikit-learn==1.5.1
scipy==1.14.0
tqdm==4.66.4
pillow==10.4.0
plotly==5.22.0

# 3D processing & visualization (compatible)
trimesh==4.4.4
open3d==0.18.0
gmsh==4.13.1
plyfile==1.1

#  Deep learning tools (installed separately for GPU compatibility)
# torch==2.0.0          ← install manually with cu118 URL
# torchvision==0.15.2   ← install manually with cu118 URL
# torchaudio==2.0.1     ← install manually with cu118 URL
# torch-geometric==2.5.3 ← install manually

#  ML/AI tools
diffusers==0.29.2
wandb==0.17.9
ninja==1.11.1.1

#  Removed (incompatible or unnecessary on Kaggle)
# dataclasses==0.8      ← built-in for Python ≥ 3.7
# vtk==9.0.1            ← heavy system deps
# chamferdist==1.0.3    ← requires custom CUDA build
# occwl==2.1.0          ← depends on OpenCascade (unavailable)
